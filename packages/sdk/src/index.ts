/**
 * `@goway.to/sdk` — the supported integration boundary for GoWay maps, Places,
 * search and routing.
 *
 * The package boundary exists from the scaffold so consumers (FairCoin, Moovo,
 * Mercaria, Homiio, Mention, Clarity) have a stable name to depend on before
 * the client itself lands. The public surface is built in #3; the domain
 * contracts it will expose already live in `packages/shared-types`.
 */

export {};
