{
  "//": "Isomorphic published package (web, React Native, Node) — extends the runtime-neutral Oxy base and turns emit on, rather than the CommonJS backend base.",
  "extends": "@oxy.so/app-preset/tsconfig/base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "noEmit": false
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
